services:
  - type: web
    name: max-drive
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: true
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    envVars:
      - key: VITE_API_URL
        value: https://your-backend-url.onrender.com # ЗАМІНІТЬ на ваш URL бекенду
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
